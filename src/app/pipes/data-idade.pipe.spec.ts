import { DataIdadePipe } from './data-idade.pipe';

describe('DataIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new DataIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
